# Next Steps

1. Add embedding backfill for `rag_transcript_chunks`.
2. Add vector search endpoint using `match_rag_transcript_chunks`.
3. Add an evaluation suite for `/api/rag/analyze` output quality.
4. Add broader `/api/rag/ask` chat once Analyze Results quality is stable.
5. Add deployment config for a separate Railway service.
